from menu_base import MenuBase  # Importing the MenuBase class from the menu_base module

class Menu(MenuBase):  # Defining the Menu class which inherits from the MenuBase class

    def display(self):  # Defining the display method within the Menu class
        self.display_menu()  # Calling the display_menu method from the MenuBase class